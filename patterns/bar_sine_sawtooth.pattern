name: Bar Sine Sawtooth
category: Chill
description: Quarter note rising sawtooth pulses (0->100% each beat) layered with a slow 2-bar sine wave that gently breathes the overall intensity
tags: pulse, ambient
palette: cool
---
(
    light("all")
    .modulate("saw", frequency=4.0, min_intensity=0.0, max_intensity=1.0)  # Rising sawtooth each beat
    .modulate("sine", frequency=0.5, min_intensity=0.3, max_intensity=1.0)  # 2-bar sine envelope
    .color(palette.random_hold(8))
)
