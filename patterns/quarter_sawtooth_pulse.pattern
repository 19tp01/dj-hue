name: Quarter Sawtooth Pulse
category: Chill
description: Sawtooth decay from 100% to 80% every beat
tags: pulse
palette: neon
---
(
    light("all")
    .modulate("saw", frequency=4.0, min_intensity=1.0, max_intensity=0.4)
    .color(palette.random_hold(8))  # New random color other every bar
)
