name: Tidal
category: Ambient
description: Two overlapping sine waves (4-bar and 8-bar) multiply together for an organic tidal rhythm
tags: ambient, slow
palette: ocean
---
# Stacked modulators multiply together
# Creates complex organic movement like ocean tides
(
    light("all")
    .modulate("sine", frequency=0.25, min_intensity=0.5, max_intensity=1.0)
    .modulate("sine", frequency=0.125, min_intensity=0.4, max_intensity=0.7)
    .color(palette.cycle_hold(32))
)
